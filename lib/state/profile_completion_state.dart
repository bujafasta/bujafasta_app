import 'package:flutter/material.dart';

/// 🔔 Global profile completion state
/// true  = profile complete
/// false = profile NOT complete
final ValueNotifier<bool> profileCompleteNotifier =
    ValueNotifier<bool>(true);
